import React from 'react';
import { StyleSheet, Text, View } from 'react-native';

const SignupScreen = () => {
    return (
        <View>
            <Text style={{fontSize: 48, textAlign: 'center'}}>Signup Screen</Text>
        </View>
    );
};

const styles = StyleSheet.create({})

export default SignupScreen;